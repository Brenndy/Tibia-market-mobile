# Vercel proxy URL for the Tibia Market API
# Used in native apps and web dev mode to avoid direct API rate limits
# In web production mode, the app uses /api/tibia (Vercel rewrites handle routing)
EXPO_PUBLIC_API_PROXY_URL=https://your-vercel-deployment.vercel.app
